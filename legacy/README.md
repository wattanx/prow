# legacy

The original TypeScript / Ink implementation of prow, kept here for reference.

Starting from v1.0.0, prow was rewritten in Rust (see the project root). This
directory is no longer built or tested as part of the release pipeline.
